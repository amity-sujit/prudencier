# remote_theme: startbootstrap/startbootstrap-creative-jekyll
# plugins:
#   - jekyll-remote-theme # this plugin is required for GitHub Pages to use remote themes
# Site settings
title:
email:
description: > # this means to ignore newlines until "baseurl:"
baseurl: "" # the subpath of your site, e.g. /blog/
url: "" # the base hostname & protocol for your site

twitter_username:
github_username:

# Build settings
markdown: kramdown
